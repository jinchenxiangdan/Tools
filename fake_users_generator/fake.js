const fetch = async() => {
	try {
		// some thing
	} catch (error) {
		console.log('Error on fetching', error)
	}
}




const targetFile = "example_data.json";

sjd = {
    "id": {
        "current_rank_score": 300
    }
}

import fs from 'fs';
var data = {
};





fs.writeFile(targetFile)